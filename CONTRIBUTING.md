# Руководство по внесению вклада

## Модель ветвления (Git Flow)
1. `master` — стабильная ветка для релизов.
2. `feature/*` — ветки для новой функциональности.
3. `hotfix/*` — срочные исправления в `master`.
4. `release/*` — подготовка к выпуску.

## Процесс Code Review
1. Создайте PR из своей ветки в `master`.
2. В описании укажите задачу, изменения и результаты тестов.
3. Назначьте ревьюверов.
4. Для слияния требуется минимум один аппрув.

## 🚀 Процесс создания Pull Request (PR)

Pull Request (PR) — это основной механизм для внесения изменений в проект. Он позволяет предложить свои изменения, обсудить их с командой и влить код в основную ветку после проверки.

### Шаг 1: Подготовка изменений
1.  Убедитесь, что вы работаете в актуальной ветке `main` вашего форка и создали новую тематическую ветку для своей задачи:
    ```bash
    git checkout master
    git pull upstream master  # Синхронизация с основным репозиторием
    git checkout -b feature/name-of-your-feature
    ```
2.  Внесите необходимые изменения в код, следуя стандартам проекта.
3.  Закоммитьте изменения с понятным сообщением:
    ```bash
    git add .
    git commit -m "feat: добавляет новый алгоритм X"
    ```
4.  Отправьте ветку в **ваш форк** на GitHub:
    ```bash
    git push origin feature/name-of-your-feature
    ```

### Шаг 2: Создание Pull Request на GitHub
1.  Перейдите на страницу **вашего форка** репозитория на GitHub.
2.  Система часто сама предлагает создать PR для только что отправленной ветки — нажмите кнопку **"Compare & pull request"**[citation:1][citation:4]. Если такого предложения нет, перейдите на вкладку **"Pull requests"** и нажмите **"New pull request"**.
3.  Настройте сравнение веток[citation:1]:
    *   **base repository**: выберите основной репозиторий (напр., `Goodonic/permutations_library`) и ветку, В КОТОРУЮ вливаются изменения (у нас это develop).
    *   **compare**: выберите вашу ветку с изменениями.
4.  Заполните заголовок и описание PR[citation:1]. В описании укажите:
    *   **Что и зачем было изменено.**
    *   **Ссылку на связанную Issue** (например, `Closes #10`)[citation:1].
    *   **Как вы протестировали свои изменения.**
5.  Нажмите **"Create pull request"**. Если работа еще не завершена, можно создать **"Draft pull request"**[citation:10].

### Шаг 3: Процесс Code Review
*   После создания PR ревьювер изучит ваш код. Он может оставить комментарии или запросить изменения.
*   Чтобы внести правки, просто добавляйте новые коммиты в свою ветку и отправляйте (`git push`) — они автоматически появятся в PR[citation:1].
*   Обсуждение ведется прямо во вкладках PR на GitHub[citation:10].
*   После устранения всех замечаний и получения необходимых одобрений (**approvals**) ответственный участник выполнит слияние (**merge**).

### ✅ Рекомендации для хорошего PR
*   **Держите PR небольшими** — так их легче и быстрее ревьювить[citation:4].
*   **Проверяйте свой PR сами** перед отправкой[citation:4].
*   **Давайте четкий контекст** в описании: цель изменений, что было сделано, ссылки на связанные задачи[citation:4].